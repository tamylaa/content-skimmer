// Placeholder for future enrichment modules (entities, topics, etc.)
export async function extractEntities(text: string): Promise<string[]> {
  // TODO: Use AI or regex for entity extraction
  return [];
}

export async function extractTopics(text: string): Promise<string[]> {
  // TODO: Use AI or keyword extraction
  return [];
}
